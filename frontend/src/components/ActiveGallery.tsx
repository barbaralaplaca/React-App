import Card from './Card'
import { Bootcamp, CardProps, Developer, Instructor } from './types'

export const ActiveGallery = (props: CardProps) => {
    const { instructors, developers, bootcamps, handleDelete } = props;

  return ( 
        // className="bootcamp" key={bootcamps}>
        //   <Card 
        //   bootcamps={bootcamps.filter((bootcamp: Bootcamp) => bootcamp.id === activeGallery.id)}
        //   instructors={instructors.filter((instructor: Instructor) => instructor.bootcampId === activeGallery.id)}
        //   developers={developers.filter((developer: Developer) => developer.bootcampId === activeGallery.id)}
        //   handleDelete={handleDelete}/>
        <div>
        </div>
        )} 
